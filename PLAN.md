# Jinger Pixel Illustration — 第一版方案

> 日期：2026-08-09（修订：补回开源可用的「创建角色」主流程）  
> Skill 名：`jinger-pixel-illustration`  
> 作者角色 / 画风锁参考：Jinger（不对使用者提供「一键体验 Jinger」）  
> 目标：开源给其他人用。先问有没有现成 IP；没有就用照片创建高清像素角色并等待确认；确认后再做长文配图或知识卡片。  
> 当前 Agent 不能生图时，必须能输出完整提示词、参考图路径、卡片 HTML 和保存计划，不假装已经出图。

---

## 1. 一句话

这是一个**可复用的高清像素个人 IP 插图 Skill**：

1. 先固定「画的是谁」（现成 IP / 照片创建）
2. 再输出两种成品：
   - **长文配图**：16:9 横图，一套里**必须有封面 + 正文配图**；封面/主题用暖色简单像素场景，正文解释默认白底迷你剧场
   - **知识卡片**：3:4 竖图，奶油米白，人物和文字一起演戏，截图就能发

别人的 IP skill 多是简笔画 / 扁平 3D / 小黑手绘，而且通常只做长文配图。这套的差别是：**高清像素 + 双形态 + 卡片必须人字互动 + 开源可创建自己的角色**。

静儿自己已经有 Jinger，本地会直接走「已确认角色 → 配图」。其他人第一次用，必须先走角色流程。

---

## 2. 已锁定决策

| 项 | 决定 |
|---|---|
| 开源 | 要。工作流给别人用；Jinger 角色资产单独授权，不默认送给别人当自己的脸 |
| 这次做什么 | **先问成品类型**：只做长文配图 / 只做知识卡片 / 两套都要。用户没说清时不要两套一起出 |
| 角色从哪来 | 没有 confirmed 角色时再问：A 现成形象 / B 参考素材创建。素材不限真人照：宠物、表情包、动漫角色、品牌 IP 都可以。**不提供试用示范角色** |
| 新角色画风 | 一律高清像素（与 Jinger 像素密度同档），不画成 8-bit、简笔画、3D 公仔、写实真人 |
| 两种形态 | 同一角色、同一像素密度；版式不同 |
| 长文 16:9 | 一套必须包含 **封面图 + 正文配图**。封面暖色简单像素场景，底色可与正文不同（相同也可以）；**同一篇正文图必须统一底色、边框、风格**。构图按这一张要表达什么写进 prompt，不设默认/非默认开关 |
| 知识卡片 3:4 | 奶油米白完整卡片；整张生图像素场景；步骤页用完整短句+编号 |
| 卡片上的字 | **整张由生图模型画完**。迷你剧场极少字；知识卡/说明图用 6–14 字完整短句。HTML 只应急。水印仍后盖 |
| 流程说明图 | 第三种构图：编号清单+箭头+信息设计优先。学 Fish 的信息结构，不学 doodle。**字大、人小；能用文字/表格/箭头说清就不必每格都有人。** 见 `explainer-workflow.md` |
| 英文像素标题 | 只出现在封面和强调页 |
| 中文/小字 | 可读性优先，不用像素字体 |
| 角色出场（长文） | 迷你剧场必须演核心动作，去掉角色图就不成立；说明图角色可选，默认点缀或不出场 |
| 角色出场（卡片） | **中度互动**：拿牌子/小气泡、靠在文字块上、举起某条要点；禁止纯文字堆叠 |
| 记忆点 | **每个角色**确认后锁死神态、剪影、标志色/花纹/服装；第一版不换到认不出 |
| 长文模式 | 自动选「流程拆解」或「核心动作」；怎么用/清单类改说明图 |
| 卡片版式 | 随页型变；页型是构图配方，不是套 HTML |
| 默认数量 | 长文：**1 张封面（另算）+ 按信息密度的正文配图**（短文 1–3，中文 3–6，长文 4–8，正文上限 8）；知识卡片默认 6–8 张，**最多不超过 9 张**；用户指定时听用户的，但卡片仍不超过 9 |
| 生成节奏 | 长文：每张先写 visual plan（插哪段 / job / 精确文案）再出图；知识卡片先出 shot list，确认后再做 |
| Markdown | 默认只报建议插入位置；明确说「插入」才改文件，并先备份 |
| 水印 | 每张图右下角，小而不抢眼；**文案写在该角色的 character-spec 里**，不要写死成静儿的号 |
| Jinger 默认水印 | `@讨厌吃Ginger的Jinger` |
| 和现有小红书项目 | 独立 skill；先不接入 Vox 流水线；知识卡片不走 HTML 叠字 |
| 静儿本地 | Jinger 已是 confirmed，不重复创建 |
| 三视图 | **可选**。必须资产是干净全身 + 半身 + spec；确认不依赖设定板 |
| 公开/私有 | 公开仓库 = 方法 + 画风锁 + 示范设定；用户照片/角色包/成图在本地 `.jinger-pixel-assets/` |

---

## 3. 和三份参考 skill 学什么、不学什么

### 学 Punk IP
- **上传照片 → 设定板 + 干净参考图 + 人物规范 → 等待确认 → 再配图**
- 草稿角色不能配图，也不能设为当前角色
- 按认知锚点配图，不按章节平均塞图
- 两种模式：流程拆解 / 核心动作
- 本地角色包 + 可切换多个角色
- 无生图时只输出提示词和保存计划
- 默认不改原文；角色资产不覆盖，用 `-v2` / `-v3`

### 学 Ian 小黑
- 角色必须参与核心动作，不能当角落贴纸
- 先提炼认知锚点，再发明一眼能懂的物理隐喻
- 一张图只讲一件事

### 学 EverettFish
- 用参考图锁画风，不要只靠文字 prompt
- 无生图时给完整 fallback

### 明确不学
- 不学简笔画断续描边、豆豆眼、扁平 3D 公仔、小黑怪诞线稿
- 不学 Vox 黄黑杂志拼贴
- 第一版不做 8-bit、不做换装、不做像素分镜第三种模式
- **要学** Punk 的创建主流程，只是画风改成高清像素，并多一条「现成 IP 导入」

---

## 4. 每次开始：先问「这次做什么」，再解决「画谁」

**硬规则：不要默认两套一起生成。**  
用户这句话里如果已经说清（例如「给这篇文章配图」「做小红书知识卡」「两个都要」），就不要再问。没说清时，先问成品类型，再进入角色/生成。

### 4.0 首次 / 用户没说清成品类型时的引导

```text
欢迎使用 Jinger Pixel Illustration。

这个 Skill 可以把一个固定的高清像素角色，做成下面两种图（先选这次要哪一种，不会默认两套都出）：

1. 16:9 长文配图（必含封面 + 正文图；封面暖色场景，正文默认白底迷你剧场）
2. 3:4 知识卡片（奶油米白，人物和文字一起演戏，可直接发）

请先选这次要做的：
1. 只要长文配图
2. 只要知识卡片
3. 两套都要

——

如果还没有确认过角色，请再选角色从哪来：
A. 我已经有现成形象（像素图、插画、设定板都行，请发图）
B. 我有参考素材，想做成高清像素角色
   （真人照、宠物、表情包、动漫角色、品牌 IP 都可以）

选 B 的话，请先看「参考素材要求」。
```

说明：

- 上面「1/2/3」和「A/B」可以一次问完，但 **1/2/3 必须问到**（除非用户已经说清）。
- 已有 confirmed 当前角色：只报角色名，再问 1/2/3（若用户已说清则不问）。不要每次重问 A/B。
- **不要出现「试用示范角色 / 启用内置角色」选项。** 捆绑像素图只用来锁画风密度，不是给别人当脸用的。
- 选了「3. 两套都要」才两条路线都跑；选 1 或 2 就只做那一条。
- 没有 confirmed 角色时，即使已经选了 1/2/3，也先走角色创建/确认，**确认前不出成品图**。

### 4.1 路线 A — 导入现成形象

用户发来已经是角色/IP 图的素材（像素图、插画、设定板、吉祥物设计稿）。

1. 判断类型：
   - **已经是高清像素** → 直接提炼特征，补齐缺失资产
   - **非像素插画 / 三视图 / 吉祥物设计** → 以它为身份参考，**转成高清像素**
   - **真人照、宠物照、表情包原图、随手拍** → 改走 4.2
2. 问角色名、水印、气质词、`character_type`
3. 生成/补齐角色包，状态 `draft`
4. **停下来等人确认**

静儿的三张图走这条：像素半身 + 像素全身锁身份和画风；非像素三视图只核对角和配件。

细则见 `references/ip-builder.md`。

### 4.2 路线 B — 从参考素材创建

素材不限真人自拍。宠物、表情包、动漫角色、品牌 IP 都可以。重点是高清复古像素，抓住神态和记忆点。

创建前必须展示「参考素材要求」（按类型：真人 / 宠物 / 表情包 / 动漫 / 品牌）。表情包如果只有头，先问要不要补全身，不要偷偷编衣服。

一次问完：角色名、水印、气质词、`character_type`。

生成目标：

> 高清复古像素角色。颗粒细，有轻微体积，像独立游戏立绘。抓住神态和记忆点，不是写成实照片，不是 8-bit，不是简笔画，不是 3D 盲盒。

只保留参考里看得见的标志特征。画风锁仍用捆绑示范像素图，只学密度，不学示范身份。

细则见 `references/ip-builder.md`。

#### 角色包文件

| 文件 | 要求 | 是否必须 |
|---|---|---|
| `character-reference-clean.png` | **干净主锚点**：纯白或透明底，单主体；默认全身，表情包头身 IP 可以是完整头身 | 必须 |
| `character-bust.png` | 干净半身/头肩（宠物=头身特写），锁神态和头上的标志特征 | 必须 |
| `character-spec.md` | 只写可观察、可复用的视觉特征 + 角色名 + 水印 + 气质词 + 签名剪影。不写猜测的年龄、职业、民族等 | 必须 |
| `character.json` | slug、状态 `draft`/`confirmed`、文件路径、创建时间 | 必须 |
| `character-sheet.png` | 竖版设定板：一个主全身像 + 正面 / 左侧 / 背面 / 右侧。仅设定板可用浅底和方向标签 | **可选** |

有三视图时，四视图必须互相一致。干净参考图**不得**把设定板排版复制进去。

#### 确认门闩（硬规则）

- 展示干净全身 + 半身（有三视图就一起展示），问：确认，还是改哪一处？同时可问要不要可选三视图。
- 一次只改一个主要问题（先改脸，或先改帽子，不要同时改五样）
- 旧文件不覆盖，存 `-v2` `-v3`
- 用户没说「确认 / 定稿 / 就用这个」之前：
  - 不能配长文
  - 不能做知识卡片
  - 不能设为当前角色
- 同时丢了照片和文章：先创建并停在确认，**不要提前配图**

### 4.3 Jinger 资产怎么用（不是一条用户路线）

- 仓库里的 Jinger 图 = **画风锁参考**（像素密度、描边、上色），外加静儿本人的角色规范。
- 开源使用者只能走 A 或 B，做出**自己的**角色。禁止把 Jinger 设成当前角色去出图。
- 静儿本地若已经有 confirmed Jinger，直接用，不必重走 A/B。
- 授权：工作流可 MIT；**Jinger 角色图单独 LICENSE-ASSETS**。

### 4.4 角色管理

- 列出 / 切换 / 查看当前角色
- 只能激活 `confirmed`
- 可有多个角色（自己、搭档、不同账号）
- 运行目录优先：当前项目 `.jinger-pixel-assets/`；没有可写项目时回退到 `~/.jinger-pixel-assets/`

---

## 5. 内置角色 Jinger（作者包）

来源（复制进 skill，不覆盖静儿原文件）：

| 原文件 | 用途 |
|---|---|
| `小红书笔记生成/avatar.png` | 高清像素半身，脸和帽子主锁图 |
| `小红书笔记生成/avartar_fullbodyshot.png` | 高清像素全身，服装/鞋/包主锁图 |
| `小红书笔记生成/avartar_panorama.png` | 非像素三视图 + 配件特写，**只核对角和配件，不锁画风** |

### Jinger 固定身份

- 名字：Jinger
- 水印：`@讨厌吃Ginger的Jinger`
- 气质：自由随性街头女孩，冷静、不卖萌
- 发型：长卷发，暖棕/深棕
- 帽子：trucker 帽，米色前幅 + 黑帽檐，前幅 `eee`
- 上衣：浅灰露肩短袖
- 裤子：深色喇叭牛仔裤，大腿做旧/破洞
- 腰带：黑色铆钉 + 银色大扣
- 鞋：厚底黑色短靴
- 配件：白色无线耳机、长黑绳圆环吊坠、黑色肩包
- 只允许改：动作、姿态、朝向、视线、轻微表情
- 其他人用照片创建时：**不要长成示范角色**，只学像素密度

其他用户确认后的角色，同样「锁自己照片里的那一身」，规则与上相同，只是内容换成他们的特征。

---

## 6. 视觉 DNA（配图阶段，所有角色共用）

### 共同规则

- 高清像素；不是 3D 渲染，不是简笔画，不是 8-bit
- 当前角色永远同一身
- 右下角水印 = 当前角色 `character-spec` 里的文案；**排版叠上去，禁止让 AI 画**
- 水印小、浅、不抢主内容

### 长文配图 16:9

一套长文图**必须同时有**：

1. **封面图 / 主题背景**：1 张，16:9  
2. **正文配图**：按信息密度决定张数，16:9，插在文章中间做解释  

缺封面或缺正文图，都不算完整交付。

**正文配图张数（不含封面）**，跟 Punk / 小黑同一思路：按信息密度，不按章节平均塞，不为凑数而画。

| 内容规模 | 正文配图 | 另加封面 | 整套合计 |
|---|---:|---:|---:|
| 单个观点或约 500 字以内 | 1 | +1 | 2 |
| 约 500—1500 字 | 2—4 | +1 | 3—5 |
| 约 1500—3500 字 | 3—6 | +1 | 4—7 |
| 约 3500 字以上 | 4—8 | +1 | 5—9 |

- 正文默认不超过 **8** 张；整套（封面+正文）默认不超过 **9** 张。
- 参考：Punk 正文 1–8（3500 字以上 4–8）；小黑默认 4–8，长文不超过 9；EverettFish 默认 5。我们多一张必出封面，所以正文表比 Punk 略放宽中段，上限仍对齐他们。
- 用户指定数量时听用户；但封面仍不能省。指定「5 张」时，理解为 1 封面 + 4 正文，除非用户说「只要正文不要封面」。

| 用途 | 背景 | 密度 |
|---|---|---|
| 封面 / 主题背景 | **暖色简单像素场景**：奶油纸 / 浅木桌 / 软木板，可加深棕像素细边框；桌上 1–3 个小物件。对齐 `style-references/warm-pixel-scene-16x9.png` | 可以稍满：人物 + 少量短标签 + 桌面；不要把文章段落画进图里 |
| 正文解释图 | **默认白底迷你剧场**：白底 + 一点点像素地面/接触阴影 | 一张一个观点；物件 1–3 个；图内 AI 字极少（2–6 字标签） |

仍然禁止：

- 写实摄影背景、窗外风景、完整大房间巡游、天空大海
- 霓虹渐变、科技 HUD、Vox 黄黑拼贴
- 把正文图塞成「十个名词白皮书海报」（那张参考图的信息密度只给封面/主题用）
- PPT 流程图外观

**流程拆解**（主要用于正文）：3–5 节点；角色可分身，每个分身都在做事；从左到右，节点之间要透气。  
**核心动作**（正文或封面钩子）：一个角色 + 一个核心物件 + 一个动作 + 一个可见结果。

### 知识卡片 3:4

- 画幅 3:4（建议 1242×1656 或同等）
- 底色：奶油米白 / 浅暖灰 / 浅木桌气质（可学参考图的暖纸板，不要纯白无菌，也不要深色 Game Boy）
- 点缀色从该角色衣服里取 3 个左右，写进 `character-spec` 的 `accent_colors`
- 像素出现在：角色/小场景、封面英文大标题、少量像素装饰
- 中文用可读字体
- **禁止纯文字堆叠**；每页角色都必须中度人字互动
- 英文像素标题仅封面 + 至多 1 张强调页

---

## 7. 知识卡片页型（第一版 6 套模板）

一套默认 6–8 张，**最多不超过 9 张**，从下面选：

| 页型 | 干什么 | 角色互动 |
|---|---|---|
| 封面 cover | 一句话钩子 | 和英文像素标题/中文副标题互动 |
| 痛点 pain | 读者卡在哪 | 指着痛点句，或被某句话卡住 |
| 概念 concept | 一个判断/地图 | 举起关键词，或站在结构节点上 |
| 步骤 steps | 3–5 步怎么做 | 分身或依次推动步骤块 |
| 对比 compare | 错 vs 对 | 站在「对」的一边，推开「错」的一边 |
| 避坑 pitfall | 常见误区 | 拦住/剪断错误做法 |
| 立刻试 cta | 现在就能做的小动作 | 把 CTA 递出来 |

封面必有；最后一页默认 `立刻试`。

---

## 8. 工作流总图

```text
用户没说清成品类型？ → 先问：只要长文 / 只要卡片 / 两套都要
    ↓
没有 confirmed 角色？ → 再问：A 现成 IP / B 照片创建（不要提供试用示范角色）
    ↓
生成或补齐角色包（draft）→ 用户确认（角色确认是强制停顿）
    ↓
confirmed + 当前角色
    ↓
按用户选择跑路线：
  1 → 只做长文配图
  2 → 只做知识卡片
  3 → 先长文再卡片，或按用户指定顺序；仍然不要偷偷少做或多做
```

### 8.1 长文配图

前提：当前角色 `confirmed`。

1. 读完整文章。读不完就停。
2. **固定产出**：1 张封面/主题图 + 按上表决定的正文解释图（封面不能省；正文按信息密度，默认上限 8）。
3. 封面用暖色简单像素场景；正文图默认白底迷你剧场，每张选流程拆解或核心动作。
4. 方案不单独等人确认，直接生成或 fallback。
5. 身份参考默认用 `character-reference-clean`；只有侧面/背面才加设定板，并禁止把设定板背景画进成图。
6. 生成后叠**该角色**水印，存 `output/articles/<slug>/`（封面建议 `00-cover.png`，正文 `01-…` 起）。
7. 交付：封面用途 + 每张正文图的用途、路径、建议插入位置。默认不改原文。

### 8.2 知识卡片

前提：当前角色 `confirmed`。

1. 读完整材料。
2. **先出 shot list，等人确认。** 默认 6–8 张，最多 9 张。
3. 确认后：每张用生图模型一次画完 3:4 像素卡（角色+场景+短文字），再后盖水印。
4. 无生图：只交 shot list + prompt；HTML 占位仅在用户明确要求时使用，并标明不是成品画风。
5. 存 `.jinger-pixel-assets/cards/<slug>/`：`shotlist.md` + `images/`。

### 8.3 Markdown 插入（仅用户明确要求）

先备份；插在相关段落后；不插进句子/列表/引用块中间。

---

## 9. 无生图 fallback

诚实说：

> 现在不能直接生成像素图。我会把角色创建或配图的完整 prompt、参考图路径、卡片 HTML 和水印计划准备好；有生图能力后再按同一套规范出图。

创建角色阶段没生图：仍然写出 `character-spec.md` 草稿 + 设定板/全身/半身的 prompt + 照片检查结果，等用户去别的工具出图后再导入确认。

配图阶段没生图：长文给 prompt；卡片给 shot list + prompt。

---

## 10. Skill 文件结构

项目里已收的画风参考（做 skill 时再打进 `assets/style-lock/`）：

- `style-references/warm-pixel-scene-16x9.png`：只给**长文封面 / 主题背景**学气质、桌面、边框。正文解释图用白底迷你剧场，不要学这张的高信息密度，也不要学「AI 在图里写大段中文」。

仓库根目录 = Skill 根目录（GitHub 才能显示 README）：

```text
.
├── README.md
├── SKILL.md
├── LICENSE
├── LICENSE-ASSETS                 # 示范角色图单独授权
├── PLAN.md
├── references/
│   ├── ip-builder.md              # 照片要求、创建、导入、确认门闩、可选三视图
│   ├── character-package.md       # 本地角色包结构、状态、切换
│   ├── character-spec.md          # 通用角色设定模板（不是作者本人 Bible）
│   ├── concept-visual-language.md # 文章概念 → 画面
│   ├── style-dna.md
│   ├── article-workflow.md
│   ├── card-workflow.md
│   ├── card-templates.md
│   ├── prompt-templates.md
│   └── qa-checklist.md
├── examples/
│   ├── jinger/                    # 作者示范设定（配合 assets/ 画风锁）
│   └── example-character/         # 虚构 Mina 填写示例（暂无图）
├── assets/
│   ├── character/                 # 画风锁参考，不对用户一键启用
│   │   ├── jinger-pixel-bust.png
│   │   ├── jinger-pixel-fullbody.png
│   │   └── jinger-turnaround.png
│   └── style-lock/
├── templates/                     # 仅无生图应急占位，不是目标气质
├── scripts/
│   ├── install.mjs                # npx 安装到 Cursor / Claude / Codex
│   ├── character_registry.py      # register 时 --sheet 可选；可 attach-sheet
│   └── bootstrap_jinger.py        # 仅作者
└── .jinger-pixel-assets/          # 本地运行数据，不进 GitHub
```

用户自己的角色和生成图**默认不进 GitHub**，只留在本地 `.jinger-pixel-assets/`。

### 建议的 description（开源触发用）

> 高清像素个人 IP 插图 Skill：可从照片或现成形象创建并确认角色，再生成 16:9 长文配图或 3:4 可分享知识卡片（人物与文字互动，文字排版叠加）。Use when 用户提到个人 IP、像素配图、角色设定、长文插图、16:9、知识卡片、3:4、小红书卡片、Jinger，或要把观点画成固定角色插图。

---

## 11. QA 硬清单

1. 是不是高清像素，而不是简笔画 / 3D 公仔 / 8-bit / 写实照片？  
2. 当前角色是不是同一张脸、同一身衣服、同一标志配件？  
3. 有没有误画成画风锁示范角色（仅当当前角色不是该示范时）？  
4. 长文套图是否同时有封面 + 正文图？封面是不是暖色简单像素场景？正文是不是白底迷你剧场、一张一个观点？  
5. 卡片是不是一整张暖色像素插画？角色是否生活在场景里？有没有做成 HTML 排版海报？  
6. 中文长字和水印是不是排版叠的？AI 图内是否几乎没长字？  
7. 英文像素标题是不是只在封面/强调页？  
8. 右下角水印是不是**当前角色自己的文案**，小而不抢眼？  
9. 一张图是不是只讲一件事？  
10. 成图有没有误带设定板标题、四视图、色板？  
11. 未确认的 draft 角色有没有被拿去配图？（有则违规）

---

## 12. 第一版明确不做

- 换装、季节差分身  
- 8-bit / Game Boy 整屏  
- 像素分镜小漫画第三模式  
- 接入现有小红书 Vox 流水线  
- 让 AI 画大段中文或水印  
- 自动 git commit / 自动发小红书  
- 根据照片猜测年龄、职业、民族、健康等敏感信息  
- 把用户照片上传到公开仓库  
- 给使用者提供「试用示范角色 / 启用内置角色」入口  

第一版**要做**：照片创建 + 现成 IP 导入 + 确认门闩。静儿本人日常使用时跳过创建，不等于 skill 里删掉这条主流程。

---

## 13. 验收标准

1. 无当前角色时，会先问 A/B（现成 IP / 照片创建），而不是直接配图；也不会出现「试用示范角色」。  
2. 照片不合格时会要补图，不会拿一张大头照硬生成全身。  
3. 创建结果含干净全身 + 半身 + spec；三视图可选；未确认不能配图。  
4. 静儿本地能直接用已确认的当前角色走长文 / 卡片。  
5. 别人的水印不会变成作者示范角色的号。  
6. 无生图时仍能交 prompt / HTML 预览 / 角色 spec 草稿。  
7. 开源说明里示范角色资产与工作流授权分开。

---

## 14. 落地状态（2026-08-09）

v1 Skill 已提到仓库根目录（`README.md` / `SKILL.md` 在根上），方便 GitHub 展示和直接安装。  
静儿本地 `.jinger-pixel-assets/` 已把 Jinger 登记为 confirmed。

### 14.1 修订（2026-08-10）

- 公开 `references/character-spec.md` = 通用模板；作者示范设定在 `examples/jinger/`；虚构示例 Mina 在 `examples/example-character/`
- 新增 `concept-visual-language.md`；prompt 改为短模块组装；QA 含 PASS/REVISE/REGENERATE + 缩略图/系列
- 三视图改为可选；`character_registry.py` 支持无 sheet 注册和 `attach-sheet`
- `SKILL.md` / `style-dna.md` 把「项目名 / 画风锁」和「用户当前角色」拆开，不再把示范角色写进主流程
- 角色来源不限真人照：宠物、表情包、动漫、品牌 IP 都可以；锁的是神态和记忆点
- 2026-08-10 试跑说明图失败后补第三种模式 `explainer-workflow.md`：怎么用/提供得到必须用编号短句+箭头，禁止迷你剧场两字标签和呆站指板子

下一步试跑：

1. 静儿用已确认角色配一篇长文（可无生图，先出 prompt + 插入建议）
2. 做一套知识卡片 shot list（整张生图像素场景，不把 HTML 当目标）
3. 模拟新用户用一张照片 / 宠物图 / 表情包，走创建引导（含可选三视图）
