# Author demo: Jinger

这是作者静儿的像素角色，也是本 Skill 的 **画风锁**（颗粒、密度、上色）。

- 图在 `assets/character/` 与 `assets/style-lock/`
- 授权见仓库根目录 `LICENSE-ASSETS`：可学画风，不可当成自己的脸或账号形象
- 开源用户请走「照片 / 现成 IP → 创建自己的角色」，不要 `bootstrap_jinger.py`，不要「用 Jinger 体验」
